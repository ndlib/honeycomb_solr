require 'spec_helper'

describe HoneycombSolr do
  it 'has a version number' do
    expect(HoneycombSolr::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
